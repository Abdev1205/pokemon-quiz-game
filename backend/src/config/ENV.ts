const PORT = process.env.PORT;
const FRONTEND_URL = process.env.FRONTEND_URL;
const POKE_API_URL = process.env.POKE_API_URL;
const POKE_IMG_URL = process.env.POKE_IMG_URL;

const ENV = {
  PORT,
  FRONTEND_URL,
  POKE_API_URL,
  POKE_IMG_URL,
};

export default ENV;
